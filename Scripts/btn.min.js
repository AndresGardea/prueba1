const BtnAnim=()=>{const menuBtn=document.querySelector("#menu-btn");let menuOpen=!1;menuBtn.addEventListener("click",()=>{menuOpen?(menuBtn.classList.toggle("open"),menuOpen=!1):(menuBtn.classList.toggle("open"),menuOpen=!0)})};

$(document).ready(function(){
 BtnAnim(BtnAnim)
});